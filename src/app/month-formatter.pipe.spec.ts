import { MonthFormatterPipe } from './month-formatter.pipe';

describe('MonthFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
